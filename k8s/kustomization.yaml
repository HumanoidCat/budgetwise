# Agrupa los manifiestos: `kubectl apply -k k8s/` aplica todo en orden
# (y no tropieza con el README, a diferencia de `kubectl apply -f k8s/`).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 00-namespace.yaml
  - 01-secrets.yaml
  - 02-postgres.yaml
  - 03-api.yaml
