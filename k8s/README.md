# Kubernetes (exploración — HU-17)

Aquí van los manifiestos de la exploración de K8s con minikube:

- `backend-deployment.yaml` + `backend-service.yaml`
- `postgres-deployment.yaml` + `postgres-service.yaml`

Objetivo de la rúbrica: demostrar la exploración de Kubernetes como estrategia
de despliegue/escalabilidad, documentando cómo se probó con `minikube start`,
`kubectl apply -f k8s/` y `minikube service`.
