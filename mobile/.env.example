# Copiar a .env — URL del backend alcanzable DESDE EL CELULAR
# En desarrollo local usar la IP de tu máquina en la red WiFi (no localhost):
EXPO_PUBLIC_API_URL=http://192.168.0.10:8000
# Para la demo con APK, la URL del backend desplegado (Render/Railway):
# EXPO_PUBLIC_API_URL=https://budgetwise-api.onrender.com
