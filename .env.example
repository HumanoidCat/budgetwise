# Puertos del HOST para docker compose (opcional). Por defecto usamos 8001/5433
# para no chocar con otros proyectos (geoguardian usa 8000/5432).
# Copiar a .env solo si necesitas cambiarlos. Los puertos internos no cambian.
API_PORT=8001
DB_PORT=5433
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
